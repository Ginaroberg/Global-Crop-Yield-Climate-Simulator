Here are the steps needed in order to run the demo version of the website locally on your own computer.

1. Install the requirements

   ```
   $ pip install -r requirements.txt
   ```
   
2. Run the app

   ```
   $ streamlit run app.py
   ```
